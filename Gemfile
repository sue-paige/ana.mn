source "https://rubygems.org"

# Jekyll — pinned for GitHub Pages local-build parity.
gem "jekyll", "~> 4.4"

# Plugins
group :jekyll_plugins do
  gem "jekyll-seo-tag", "~> 2.8"
  gem "jekyll-sitemap", "~> 1.4"
end

# Windows / JRuby niceties (harmless elsewhere)
gem "webrick", "~> 1.8"
